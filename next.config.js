const nextConfig = {
  // Remove this webpack override unless you're doing something advanced
  // webpack(config) {
  //   config.module.rules.push({
  //     test: /\.css$/,
  //     use: ['style-loader', 'css-loader'],
  //   });
  //   return config;
  // },
};

module.exports = nextConfig;